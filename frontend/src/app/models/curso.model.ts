import { userSingUp } from "./userSingUp";

export interface cursos {
      id:String,
      name: String,
      categoria: String,
      autor: userSingUp,
      createdAt: String,
      updatedAt: String
}